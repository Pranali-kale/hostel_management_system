
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Profiles</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 0;
            background-color: #f4f4f4;
        }
        h2 {
            text-align: center;
        }
        .add-button {
            display: block;
            width: 200px;
            text-align: center;
            margin: 10px auto;
            padding: 10px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 4px;
        }
        .add-button:hover {
            background-color: #0056b3;
        }
        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            background: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        th, td {
            padding: 10px;
            border: 1px solid #ddd;
            text-align: center;
        }
        th {
            background-color: #007bff;
            color: white;
        }
        .edit, .delete {
            text-decoration: none;
            padding: 5px 10px;
            border-radius: 4px;
            color: white;
        }
        .edit {
            background-color: #ffc107;
        }
        .edit:hover {
            background-color: #e0a800;
        }
        .delete {
            background-color: #dc3545;
        }
        .delete:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body>

<h2>Student Profiles</h2>

<a href="/admin/students/add" class="add-button">Add New Student</a>

<table>
    <tr>
        <th>Roll Number</th>
        <th>Name</th>
        <th>Email</th>
        <th>Phone</th>
        <th>Room Number</th>
        <th>Password</th>
        <th>Actions</th>
    </tr>
    <% students.forEach(student => { %>
        <tr>
            <td><%= student.rollNumber %></td>
            <td><%= student.name %></td>
            <td><%= student.email %></td>
            <td><%= student.phone %></td>
            <td><%= student.roomNumber %></td>
            <td><%= student.password  %></td>
            <td>
                <a href="/admin/students/edit/<%= student._id %>" class="edit">Edit</a>
                <a href="/admin/students/delete/<%= student._id %>" class="delete" onclick="return confirm('Are you sure you want to delete this student?')">Delete</a>
            </td>
        </tr>
    <% }) %>
</table>

</body>
</html>
