<!DOCTYPE html>
<html lang="en">
<head>
    <title>student deallocation</title>
    <style>
        body{
            padding: 30px;
            background-color: #f8f9fa;
            color:#333;
        }
        h1{
            text-align: center;
            color:#2c3e50;
            margin-bottom: 20px;
        }
        a{
            text-decoration: none;
            padding:8px 14px;
            border-radius: 4px;
            font-size: 16px;
            margin-left: 150px;
        }
        table{
            width: 100px;
            border-collapse:separate  ;
            background: white;
            box-shadow: 0px 3px 8px rgba(0,0,0,0.1);
            margin-top: 20px;
            position: absolute;
            /* justify-content: ; */
            align-items: center;

        }
        th,td{
            padding: 12px 15px;
            border:1px solid #dee2e6;
            text-align: center;
        }
        th{
            background-color: #007bff;
            color:white;
            font-weight:600;
        }
        #tab{
            position:absolute;
            width: 900px;
            left: 190px;
            justify-content: center;
            
        }
    </style>
    </head>
<body>
    <h1>Students with Allocated Rooms</h1>
    <a href="/admin/rooms">← Back to Room List</a>

    <table id="tab">
        <thead>
            <tr>
                <th>Name</th>
                <th>Roll No</th>
                <th>Room Number</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% students.forEach(student => { %>
                <% if (student.roomNumber) { %>
                    <tr>
                        <td><%= student.name %></td>
                        <td><%= student.rollNumber %></td>
                        <td><%= student.roomNumber %></td>
                        <td>
                            <form action="/admin/rooms/deallocate" method="POST" style="display:inline;">
                                <input type="hidden" name="studentId" value="<%= student._id %>">
                                <button type="submit">Deallocate Room</button>
                            </form>
                        </td>
                    </tr>
                <% } %>
            <% }) %>
        </tbody>
    </table>
</body>
</html>
